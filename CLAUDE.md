# CLAUDE.md

## Commit Rules

- Commit after every meaningful change. Do not batch multiple changes into one session without committing.
- Push to remote after each commit.
- Use conventional commit messages: `feat:`, `fix:`, `refactor:`, `test:`, `docs:`.
